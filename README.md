# Foxy Live releases

Public downloads for [Foxy Live](https://seedext.com), Seedext's live
transcription app, and the feed its self-updater polls. Each release carries
the update artifacts for macOS (Apple Silicon); `latest.json` on the newest
release is the updater's manifest.

No code lives here. This repository exists so that shipped apps can fetch
updates without credentials.
